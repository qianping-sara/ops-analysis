# Vite dev server proxies /api and /health to this target (default 127.0.0.1:8000)
# VITE_API_PROXY_TARGET=http://127.0.0.1:8000

# Leave empty to use same-origin + proxy. Set only if you enable CORS on the backend.
# VITE_API_BASE_URL=

# VITE_AGENT_TYPE=analysis
